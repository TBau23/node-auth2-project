// this wont have much in it for the purposes of this project
// but good practice to use this kind of thing for dynamic variables

module.exports = {
    jwtSecret: process.env.JWT_SECRET || "whispering winds whip through willows"

}